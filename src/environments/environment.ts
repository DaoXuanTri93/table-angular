import { APP_FORMAT_DATE, APP_NAME, LANGUAGE, type UiEnvironment, URL_LANGUAGES } from './environment.interface';

export const environment: UiEnvironment = {
  APP_NAME,
  STATUS: 'STAGING',
  URL_API: 'https://jsonplaceholder.typicode.com',
  URL_LANGUAGES,
  LANGUAGE,
  APP_FORMAT_DATE,
};
